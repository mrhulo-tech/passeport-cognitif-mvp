class Profile:
    def __init__(self, user_id, current_level, target_level, progress_score):
        self.user_id = user_id
        self.current_level = current_level
        self.target_level = target_level
        self.progress_score = progress_score
